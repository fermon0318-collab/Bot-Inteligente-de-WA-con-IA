# Se instala en /etc/logrotate.d/elorai — ver deploy/README.md § 8.
# El log de nginx ya lo cubre /etc/logrotate.d/nginx (viene con el paquete).
/var/log/elorai-backup.log {
    weekly
    rotate 8
    compress
    missingok
    notifempty
}
